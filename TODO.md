# 授权图片区域修复工具 TODO

## 实施记录

- 2026-07-20：完成本地 MVP 主流程，包括授权门槛、安全文件校验、分辨率降级、Konva 编辑器、遮罩历史、候选检测、单 Worker OpenCV 局部修复、结果对比和 JPEG/PNG/WebP 下载。验证通过：lint、TypeScript、20 个单元测试、生产构建、OpenCV `photo/inpaint` 运行时检查，以及 Edge 桌面/移动视口端到端冒烟测试。真实 Safari/Firefox、iOS/Android 设备、4 GB Android、低网速和 Vercel Preview 验收仍待执行。

> 技术基线：纯前端优先，不调用第三方图片修复 API。MVP 使用 Canvas、Web Worker 和 OpenCV.js 完成本地小区域修复；LaMa ONNX 作为后续可选增强。
>
> 合规基线：公开名称使用“授权图片区域修复”或“图片瑕疵修复”；不提供批处理、图片 URL 导入、公开自动化 API，禁止处理他人版权标识、平台保护标记及素材站预览标记。

## 优先级说明

- `P0`：首个可用版本的阻塞项。全部完成后才具备上线条件。
- `P1`：上线质量、移动端体验、安全和稳定性完善。
- `P2`：浏览器端开源生成式模型增强，不阻塞基础版本。
- `P3`：存在明确业务需求后才建设的服务端和高级能力。
- `DoD`：Definition of Done，该事项可以标记完成的最低标准。

## 已确定的默认参数

- [x] 产品名称：授权图片区域修复。
- [x] MVP 不接数据库、对象存储或图片修复 API。
- [x] 输入格式：JPEG、PNG、WebP；拒绝 SVG、动画 GIF、HEIC/HEIF。
- [x] 原文件上限：10 MB、1200 万像素。
- [x] 工作图上限：桌面 800 万像素，移动端或低内存设备 400 万像素。
- [x] 预览图最长边：2048 px；检测图最长边：1024 px。
- [x] 遮罩覆盖率：不超过 10%正常处理，10%-25%警告，超过 25%禁用传统自动修复。
- [x] 同时只允许编辑一张图片、执行一个修复任务。
- [x] 撤销/重做最多保留 50 个用户动作。
- [x] 用户图片、遮罩和结果只保存在浏览器内存中。

---

## P0：首个可用版本

### P0-01 项目骨架与依赖边界

- [x] 创建 Next.js 当前稳定版、React、TypeScript 项目。
- [x] 配置 lint、类型检查、生产构建和最小测试命令。
- [x] 引入 `react-konva`，只用于预览和交互层。
- [x] 选择并本地托管 OpenCV.js/WASM，不从第三方 CDN 运行时加载。
- [x] 验证所选 OpenCV.js 构建包含 `photo/inpaint`；如果不包含，仅构建 `core + imgproc + photo`。
- [x] 不引入 Redux、Zustand、Fabric.js、WebGL 编辑器、数据库 SDK 或模型服务 SDK。
- [x] 将编辑器和 OpenCV 设为按需加载，避免进入页面立即下载大资源。

**依赖：**无。

**DoD：**开发、类型检查、测试和生产构建均能成功执行；首屏不加载 OpenCV WASM；仓库中不存在外部图片 API Key。

### P0-02 授权确认与产品边界

- [x] 首次进入显示授权确认界面，未勾选前禁用文件选择。
- [x] 授权声明包含“拥有版权或已取得明确修改授权”。
- [x] 明确禁止移除他人版权标识、平台保护标记、素材站预览标记。
- [x] 明确禁止利用工具规避付费、许可和访问限制。
- [x] 说明系统无法通过技术手段判断真实版权归属。
- [x] 提供服务条款、隐私说明和投诉/下架入口占位链接。
- [x] 清空或重新开始后，根据产品决定重新确认或保留本次页面会话确认状态。

**依赖：**P0-01。

**DoD：**未确认授权时无法选择文件；确认文案可由键盘和读屏访问；页面中不存在“一键去水印”“批量去水印”等宣传文案。

### P0-03 文件校验与安全解码

- [x] 支持点击选择、拖放和移动端相册/拍照入口。
- [x] 校验文件扩展名和浏览器 MIME，仅作为初步检查。
- [x] 读取文件头并验证 JPEG、PNG、WebP 魔数。
- [x] 在完整解码前读取或探测图片尺寸，拒绝异常像素数和损坏文件。
- [x] 拒绝 SVG、动画 GIF、HEIC/HEIF、伪装扩展名和无法解码的文件。
- [x] 使用 `createImageBitmap` 解码，并提供 `HTMLImageElement` 回退。
- [x] 处理 EXIF 方向 1、3、6、8，进入编辑器前统一视觉方向。
- [x] 检测透明 PNG，并记录 Alpha 处理模式。
- [x] 为格式错误、超限、解码失败和内存不足提供不同错误提示。

**依赖：**P0-01、P0-02。

**DoD：**测试图片方向正确；损坏文件不会进入编辑器；10 MB或1200万像素以上文件被明确拒绝；文件名和扩展名不能绕过校验。

### P0-04 分辨率与内存降级

- [x] 根据原图尺寸计算工作图、预览图和检测图尺寸。
- [x] `navigator.deviceMemory <= 4` 时将工作图限制为 400 万像素。
- [x] 无设备内存信息且为触屏移动设备时采用 400 万像素上限。
- [x] 其他设备采用 800 万像素工作图上限。
- [x] 使用分阶段降采样，避免同时持有多份完整原图 RGBA 缓冲。
- [x] 降采样时明确展示最终输出尺寸，不静默降低质量。
- [x] 生成工作图后立即释放不再使用的 ImageBitmap 和临时 Canvas。
- [x] 预览图最长边限制为 2048 px，自动检测图最长边限制为 1024 px。

**依赖：**P0-03。

**DoD：**400万像素工作图在目标4GB Android设备上不会导致页面终止；用户能在编辑前看到输出分辨率；换图后旧位图被释放。

### P0-05 编辑器画布与坐标系统

- [x] 建立图像层、遮罩层和交互层，禁止把工具面板绘制进图像 Canvas。
- [x] 支持适应窗口、缩放、平移和恢复视图。
- [x] 桌面端支持滚轮缩放、空格拖动和快捷键撤销/重做。
- [x] 移动端支持双指缩放/平移；只有激活绘制工具后单指才修改遮罩。
- [x] 所有遮罩使用工作图像像素坐标，不使用屏幕坐标。
- [x] 实现屏幕坐标到工作图像坐标的逆矩阵映射。
- [x] 限制缩放范围，建议 25%-800%。
- [x] 平移边界允许查看图像边缘，但不能把画布永久拖出可视区域。
- [x] 窗口尺寸和设备方向变化后保持当前图像中心与缩放语义。

**依赖：**P0-04。

**DoD：**在25%、100%、400%和800%缩放下，同一点的绘制映射误差不超过2个工作图像像素；窗口变化不会改变遮罩位置。

### P0-06 遮罩工具与历史记录

- [x] 实现矩形选区的创建、移动、缩放和删除。
- [x] 实现添加画笔，支持可调笔刷大小。
- [x] 实现橡皮擦，移除已有遮罩。
- [x] 对画笔轨迹进行最小距离采样，避免保存过多重复点。
- [x] 一次完整指针操作只生成一个历史动作。
- [x] 实现最多50步撤销和重做；新增动作后清空重做栈。
- [x] 将矩形、画笔和擦除操作重放为工作图同尺寸的单通道二值遮罩。
- [x] 计算遮罩总像素、覆盖率、包围盒、连通区域数量和最大区域尺寸。
- [x] 空遮罩时禁用修复按钮。
- [x] 覆盖率10%-25%显示风险确认；超过25%禁用传统自动修复。

**依赖：**P0-05。

**DoD：**50步撤销/重做顺序正确；矩形和笔画可组合；擦除后的最终遮罩与预览一致；空遮罩不能启动处理。

### P0-07 图像 Worker 与 OpenCV 生命周期

- [x] 创建单一图像 Worker，同时承载候选检测和传统修复。
- [x] 设计 Worker 消息类型：初始化、检测、修复、取消、释放和错误。
- [x] 使用任务 ID 丢弃过期或已取消任务的返回结果。
- [x] 第一次需要修复时才加载 OpenCV.js/WASM。
- [x] 显示 OpenCV 资源加载、初始化和处理进度。
- [x] 优先通过 Transferable 传递 ImageBitmap/ArrayBuffer，避免大图结构化复制。
- [x] Worker 异常时可重建；编辑命令仍保留在主线程。
- [x] 清空图片或离开页面时终止 Worker 并释放资源。

**依赖：**P0-01、P0-04、P0-06。

**DoD：**OpenCV 不阻塞主线程；任务取消后旧结果不会覆盖当前图片；Worker 崩溃后用户仍可继续编辑或重试。

### P0-08 本地传统修复流程

- [x] 根据遮罩包围盒裁剪局部图像，不处理完整工作图。
- [x] 根据遮罩尺寸增加64-256px上下文，并限制裁剪边界。
- [x] 将局部遮罩膨胀2-8个工作图像像素，覆盖文字边缘。
- [x] 默认使用 Telea 修复，修复半径初始设置为3-5px并按分辨率调整。
- [x] Telea 明确失败时允许尝试 Navier-Stokes一次，不循环尝试。
- [x] 将修复局部映射回工作图坐标。
- [x] 对合成边缘羽化2-4px。
- [x] 只在羽化遮罩内使用修复像素，遮罩外始终复制工作原图。
- [ ] 大面积、人物五官、规则建筑纹理等高风险选区显示质量警告。
- [x] 处理失败后保留图片、遮罩、视图和撤销历史。

**依赖：**P0-06、P0-07。

**DoD：**小污点、日期和简单背景文字能得到可见修复结果；遮罩外像素与工作原图完全一致；失败不会清空用户编辑。

### P0-09 自动候选基础版

- [x] 在1024px检测图上执行灰度转换和轻度降噪。
- [x] 计算 Sobel 水平/垂直边缘和局部对比度。
- [x] 使用自适应阈值与形态学闭运算连接相邻笔画。
- [x] 提取连通区域，排除过小、过大和接近整图的区域。
- [x] 按高度、基线和间距组合文字状候选。
- [x] 将候选坐标映射回工作图像。
- [x] 每个候选保存类型、置信度、来源和矩形坐标。
- [x] 候选仅展示，用户必须主动接受后才进入遮罩。
- [x] 支持单个接受、全部接受、忽略和删除候选。
- [x] 未检测到候选时显示“未找到可靠候选，可继续手动编辑”。

**依赖：**P0-04、P0-07。

**DoD：**高对比日期和文字样例能产生可调整候选；复杂背景误报可删除；检测结果不会被描述为版权结论。

### P0-10 结果对比与下载

- [x] 提供原图/结果滑杆对比。
- [x] 提供结果、原图和遮罩显示切换。
- [x] “重新编辑”返回编辑状态并保留原遮罩。
- [x] 按工作图分辨率在原生 Canvas 中生成最终结果。
- [x] 透明图片导出时保留原 Alpha，不能无提示展平。
- [x] 支持 JPEG、PNG、WebP 输出。
- [x] JPEG/WebP提供质量选项，默认建议0.92。
- [x] 文件名使用原文件名加“修复结果”后缀，不记录或上传文件名。
- [x] 下载后及时撤销临时 Blob URL。
- [x] 界面反馈使用“下载已开始”，不声称浏览器已完成磁盘写入。

**依赖：**P0-08。

**DoD：**三个输出格式均可打开；输出尺寸符合工作图尺寸；遮罩外像素保持不变；连续下载不会持续增长 Blob 内存。

### P0-11 隐私清理与基础错误恢复

- [x] 不将图片、遮罩和结果写入 LocalStorage、IndexedDB 或 Cache Storage。
- [x] 换图、清空和页面卸载时撤销 Blob URL、关闭 ImageBitmap并清空Canvas。
- [x] 不接入会话回放、图片分析或会采集文件内容的 SDK。
- [ ] 错误只记录通用阶段、错误码、浏览器类型和耗时，不记录图片数据、文件名和遮罩。
- [x] 为资源加载失败、Worker失败、内存不足、编码失败提供恢复按钮。
- [x] 离开页面前若存在未下载结果，显示丢失确认。
- [x] 处理期间断网不影响已经加载的本地图片和算法。

**依赖：**P0-03至P0-10。

**DoD：**开发者工具中不存在用户图片网络请求；刷新后无法恢复用户图片；可恢复错误不会让编辑器进入不可操作状态。

### P0-12 MVP 测试与发布门槛

- [ ] 建立 JPEG、PNG、WebP、EXIF 1/3/6/8、透明图、损坏图和超限图测试集。
- [ ] 测试纯色、纹理、人物、建筑、日期、污点、小文字和大遮罩场景。
- [x] 单元测试坐标正反变换、尺寸计算、遮罩覆盖率和历史栈。
- [x] 像素测试验证遮罩外区域完全一致。
- [ ] 测试 Worker 初始化、取消、崩溃和重建。
- [ ] 测试低网速下首次加载 OpenCV 的进度和失败恢复。
- [ ] 在 Chrome、Edge、Safari、Firefox 当前版本完成桌面验收。
- [ ] 在 iOS Safari、Android Chrome 和一台4GB Android设备完成移动验收。
- [ ] 执行生产构建并部署 Vercel Preview。

**依赖：**全部 P0 功能。

**DoD：**P0 验收用例全部通过；4GB Android不因400万像素工作图崩溃；网络面板无用户图片上传；生产构建无类型错误。

---

## P1：上线质量与可控性

### P1-01 克隆图章

- [ ] 支持设置来源点和目标笔画。
- [ ] 使用固定偏移映射来源和目标坐标。
- [ ] 支持笔刷大小、硬度和透明度。
- [ ] 防止来源采样越过图片边界。
- [ ] 将克隆操作纳入统一撤销/重做命令。
- [ ] 移动端使用长按设置来源点，避免与绘制手势冲突。

**DoD：**复杂纹理下可由用户手动覆盖传统修复失败区域；高清导出与预览位置一致。

### P1-02 移动端交互完善

- [ ] 底部工具栏按钮触控区域不小于44×44px。
- [ ] 参数使用底部抽屉，不遮挡主要编辑区域。
- [ ] 双指手势期间禁止产生画笔命令。
- [ ] 浏览器地址栏高度变化时保持画布稳定。
- [ ] 横竖屏切换后保持选区、缩放中心和工具状态。
- [ ] 对长任务保持屏幕状态反馈，避免用户误以为卡死。

**DoD：**单指绘制、双指平移缩放无明显冲突；工具文字和数值不溢出；横竖屏切换不丢数据。

### P1-03 性能与内存优化

- [ ] 为遮罩命令增加周期性栅格检查点，建议每20个动作一次。
- [ ] 检测和修复阶段复用缓冲区，减少临时 RGBA 副本。
- [ ] 对长笔画进一步简化点序列，但保持曲线精度。
- [ ] 记录不含图片内容的阶段耗时和内存等级。
- [ ] 建立桌面60fps、低端移动端30fps的交互基准。
- [ ] 生成新结果前释放旧结果位图。

**DoD：**50步复杂笔画下撤销延迟可接受；连续处理5张图片内存不持续增长；主线程没有明显长时间冻结。

### P1-04 CSP、资源与依赖安全

- [ ] 配置严格 CSP，限制脚本、图片、Worker、WASM和连接来源。
- [ ] 设置 `object-src 'none'`、`frame-ancestors 'none'`和严格`base-uri`。
- [ ] OpenCV、字体、图标全部同源托管。
- [ ] 为带内容哈希的静态资源设置长期不可变缓存。
- [ ] 锁定依赖版本并执行依赖漏洞检查。
- [ ] 确认生产环境不使用 `unsafe-eval`。

**DoD：**CSP 开启后编辑、Worker、WASM和下载功能正常；页面不能被第三方站点嵌入；无高危依赖漏洞。

### P1-05 无障碍与文案

- [ ] 所有工具按钮提供可访问名称和工具提示。
- [ ] 键盘可完成文件选择、工具切换、撤销、重做和下载。
- [ ] 焦点样式可见，错误提示与对应控件关联。
- [ ] 不仅依赖颜色表达遮罩和错误状态。
- [ ] 支持 `prefers-reduced-motion`。
- [ ] 完成授权、隐私、拒绝、失败和降级文案评审。

**DoD：**核心流程可用键盘完成；读屏能识别工具状态；颜色对比和触控目标满足基本无障碍要求。

### P1-06 可选本地 OCR

- [ ] 评估 Tesseract.js 包体、语言包、速度和移动端内存。
- [ ] 只在用户点击“识别文字”后懒加载。
- [ ] OCR 结果只用于候选框和合规提示，不自动修复。
- [ ] 对版权符号、copyright、all rights reserved和已确认平台名称执行风险提示。
- [ ] OCR 超时或失败时回退到启发式检测。

**DoD：**OCR 不增加首屏包体；失败不阻塞手动编辑；识别结果不被描述为版权归属判断。

---

## P2：浏览器端 GitHub 开源模型

### P2-01 LaMa 许可与来源审查

- [ ] 核查 LaMa 上游代码许可证。
- [ ] 单独核查模型权重许可证、来源和商业使用限制。
- [ ] 记录上游仓库、commit、权重 SHA-256和下载来源。
- [ ] 审查 ONNX Runtime Web 及转换工具依赖许可证。
- [ ] 未完成许可确认前不得在生产环境分发模型。

**DoD：**代码、权重和依赖均有可追溯许可记录；产品负责人确认可用于目标服务地区和商业场景。

### P2-02 可复现 ONNX 转换

- [ ] 从已确认的上游权重生成 ONNX，不采用来源不明的社区模型文件。
- [ ] 固定转换环境和依赖版本。
- [ ] 对固定输入比较原模型与 ONNX 输出。
- [ ] 验证动态尺寸或确定固定输入尺寸策略。
- [ ] 记录模型输入归一化、遮罩语义、尺寸对齐和输出范围。

**DoD：**任意开发者可从记录的上游权重重复生成相同模型；固定样例输出差异在可接受范围内。

### P2-03 ONNX Runtime Web 技术验证

- [ ] 检测 WebGPU 能力和适配器申请失败场景。
- [ ] WebGPU 可用时优先使用 WebGPU 后端。
- [ ] WASM 仅对较小裁剪开放，避免低端设备长时间卡死。
- [ ] 评估是否需要 COOP/COEP 和 WASM 多线程。
- [ ] 确认 CSP、跨域隔离、字体、Worker和静态资源不会互相冲突。
- [ ] 同时只创建一个模型会话和一个推理任务。

**DoD：**不支持 WebGPU 时无崩溃并自动回退；模型加载、推理和释放均有明确状态；页面关闭后资源被释放。

### P2-04 LaMa 局部推理管线

- [ ] 使用遮罩包围盒加128-256px上下文生成模型裁剪。
- [ ] 使用反射填充将尺寸对齐到模型要求。
- [ ] 初期将模型输入最长边限制为512或1024px。
- [ ] 推理后将输出映射回工作图坐标。
- [ ] 仍然只在遮罩和羽化边缘内合成模型结果。
- [ ] 复杂或过大裁剪不做自动分块，先回退传统修复或提示用户缩小选区。
- [ ] 模型下载前显示预计资源大小，由用户主动确认。

**DoD：**模型不能修改遮罩外区域；模型失败可无损返回编辑状态；传统修复仍可独立工作。

### P2-05 模型上线门槛

- [ ] 桌面 WebGPU 典型局部处理目标不超过15秒。
- [ ] 在8GB桌面设备上验证峰值内存和连续任务稳定性。
- [ ] 在目标 Chrome、Edge和Safari版本验证兼容性。
- [ ] 模型文件采用内容哈希、长期缓存和完整性校验。
- [ ] 评估模型 CDN 流量和首次下载成本。
- [ ] 移动端默认关闭本地 AI，只有基准通过后再开放。

**DoD：**许可、质量、延迟、内存、兼容性和带宽成本全部通过评审；任一项不通过则不进入生产。

---

## P3：存在真实需求后再建设

### P3-01 自托管 IOPaint/LaMa GPU 服务

- [ ] 只有浏览器模型无法达到质量或稳定性要求时才启动。
- [ ] 评估 IOPaint 支持的模型、GPU显存、吞吐和许可。
- [ ] GPU服务与Vercel前端分离部署，禁止尝试在Vercel Function中运行PyTorch模型。
- [ ] 图片只保存于任务临时目录，完成或失败后立即删除。
- [ ] 设置30-60分钟兜底清理任务。
- [ ] 结果只通过短期签名地址或直接响应返回。

### P3-02 极薄 Serverless 代理

- [ ] 服务端重新校验授权凭证、文件魔数、尺寸、遮罩和覆盖率。
- [ ] API Key和GPU服务凭据只保存在服务端环境变量。
- [ ] 支持创建任务、查询状态、取消任务和统一错误码。
- [ ] 使用幂等键防止重复任务和重复费用。
- [ ] 添加单IP限流、并发限制、每日预算和熔断。
- [ ] 对高置信度平台或素材站保护标记执行服务端拒绝。

### P3-03 临时对象存储和队列

- [ ] 只有请求体限制、大文件或任务跨页面确实发生时才引入。
- [ ] 对象设置不超过1小时的自动生命周期删除。
- [ ] 元数据只保存任务状态、过期时间和通用错误码。
- [ ] 不保存原文件名、图片内容摘要或长期用户历史。
- [ ] 队列只允许单图交互任务，不开放批量生产接口。

### P3-04 高级识别能力

- [ ] 评估重复平铺候选检测。
- [ ] 评估半透明覆盖区域检测。
- [ ] 评估更准确的文字/Logo候选模型。
- [ ] 所有识别结果仍须用户编辑和确认。
- [ ] 不将模型输出描述为版权归属结论。

---

## 明确不做

- [x] 不做批量上传、批量修复或 ZIP 下载。
- [x] 不做图片 URL 抓取和远程网站导入。
- [x] 不提供公开 REST API、CLI、Webhook 或自动化 SDK。
- [x] 不做账号历史、默认云端保存或自动恢复用户图片。
- [x] 不允许任意生成式提示词改变整张图片。
- [x] 不为了支持超大图在 MVP 中实现复杂分块模型推理。
- [x] 不把浏览器端提示包装成不可绕过的版权验证。

## 开发启动前最终确认

- [ ] 产品负责人确认授权声明、禁止用途、隐私说明和投诉入口文案。
- [ ] 产品负责人确认10 MB、1200万输入像素、800万/400万工作像素上限。
- [ ] 产品负责人确认透明 PNG 保留 Alpha 的处理规则。
- [ ] 产品负责人确认遮罩10%警告、25%禁用传统修复的阈值。
- [ ] 产品负责人确认公开页面不使用“去水印”作为功能卖点。
- [x] 技术负责人确认 OpenCV.js 构建来源、版本和 `photo/inpaint` 可用性。
- [ ] 在进入 P2 前，由产品和法务共同确认 LaMa 代码与权重许可。

## MVP 总体验收

- [x] 用户必须先确认授权才能选择图片。
- [x] 用户可完成上传、预览、缩放、平移、候选查看、框选、画笔、橡皮擦、撤销和重做。
- [x] 用户可在浏览器内完成小面积修复、前后对比和下载。
- [x] 修复结果遮罩外像素与工作原图完全一致。
- [x] API Key、数据库、对象存储和第三方图片修复 API 均不存在。
- [x] 浏览器网络请求中不存在用户原图、遮罩或结果上传。
- [x] 失败、取消和Worker重建不会丢失当前编辑命令。
- [x] 移动端低内存降级、格式错误和超限场景都有清晰恢复路径。
- [ ] 生产构建、目标浏览器测试、移动端测试、安全检查和隐私检查全部通过。
